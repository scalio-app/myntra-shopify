# --- Runtime defaults for Myntra → Shopify Transformer ---
# Copy this file to .env and adjust values.

# Core defaults
DEFAULT_QTY=50
DEFAULT_GRAMS=400

# LLM settings (local OpenAI-compatible server)
LLM_PROVIDER=openai
LLM_BASE_URL=http://127.0.0.1:1234
LLM_ENDPOINT=completions
LLM_MODEL=qwen/qwen3-8b
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2048
LLM_TIMEOUT=30
LLM_RATE_SLEEP=0.2
LLM_CACHE_DIR=.cache/llm
# Prefer LLM descriptions when enabled, even if attribute-based copy exists
# LLM_PREFER=false

# Brand voice
LLM_BRAND=Zummer
LLM_AUDIENCE=Modern Indian women, 25–35

# Cloud (optional) — only if using OpenAI or another hosted provider
# OPENAI_API_KEY=
# LLM_API_KEY=           # direct key override (discouraged)
# LLM_API_KEY_FILE=      # path to a file containing the API key
# LLM_API_KEY_ENV=OPENAI_API_KEY
